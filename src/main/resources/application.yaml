server:
  port: 8080

spring:
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
  servlet:
    multipart:
      max-file-size: 20MB
  mail:
    host: smtp.gmail.com
    port: 587
  flyway:
    baseline-on-migrate: true

network-address: http://iasasc.centralindia.cloudapp.azure.com/