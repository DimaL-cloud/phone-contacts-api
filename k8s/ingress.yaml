apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress
spec:
  rules:
    - http:
        paths:
          - pathType: Prefix
            path: /phone-contacts-api
            backend:
              service:
                name: cats-api
                port:
                  number: 80